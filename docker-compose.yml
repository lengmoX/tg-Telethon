version: '3'

services:
  tgf:
    image: moleng1/tg-telethon:latest
    container_name: tgf_app
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      # 数据持久化目录Mapping data directory
      - ./data:/data
    environment:
      - TGF_LOG_LEVEL=INFO
      # 如果需要自定义 API ID/Hash (通常在首次初始化后会在数据库中，这里是可选覆盖)
      # - TGF_API_ID=123456
      # - TGF_API_HASH=abcdef...
      # - TZ=Asia/Shanghai  # 设置时区
